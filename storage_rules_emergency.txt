rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // ЭКСТРЕННЫЕ ОТКРЫТЫЕ ПРАВИЛА ДЛЯ ТЕСТИРОВАНИЯ
    // ⚠️ НЕ ИСПОЛЬЗОВАТЬ В ПРОДАКШН ⚠️
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
} 